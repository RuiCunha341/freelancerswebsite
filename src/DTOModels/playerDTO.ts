export interface IPlayerDTO {
    health: number; // current health
    maxHealth: number; // max health
    mana: number;
    attack: number;
    defence: number;
    // availableSkills?: IUsableItemOrSkill[];
    // availableItems?: IUsableItemOrSkill[];
}
